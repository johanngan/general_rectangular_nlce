function key = get_key(measurement_displacement)
    % KEY = GET_KEY(MEASUREMENT_DISPLACEMENT)
    %   Forms the map key for the measurement under some displacement
    key = mat2str(measurement_displacement);
end